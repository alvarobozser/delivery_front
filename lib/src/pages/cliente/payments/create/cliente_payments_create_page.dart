import 'package:flutter/material.dart';

class ClientePaymentsCreatePage extends StatelessWidget {
  const ClientePaymentsCreatePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('PAGOS'),
      ),
    );
  }
}
